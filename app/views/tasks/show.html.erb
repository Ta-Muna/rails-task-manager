<h1><%= @task.title %> </h1>
<h2><%=@task.details %></h2>

<p>
      <% if @task.completed %>
        This task is ✅ Completed
      <% else %>
        This task is ❌ Not completed
      <% end %>
</p>

<%= link_to "Edit the Task", edit_task_path(@task) %>
<%= link_to "back to all the tasks", tasks_path(@task) %>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
